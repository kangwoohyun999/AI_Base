## 개념

### 1. 인공지능의 역사와 '사전 학습(Pre-training)'의 의미 (2주차)
* **기울기 소실(Vanishing Gradient):** 신경망이 깊어질수록 뒤쪽(출력층)에서 계산된 오차가 앞쪽(입력층)까지 전달되지 못하고 0에 가까워져 학습이 멈추는 현상입니다.
* **RBM(제한된 볼츠만 머신):** 힌튼 교수가 제안한 해결책으로, 정답이 없는 데이터를 먼저 '구경'시켜서 데이터의 특징을 미리 파악하게 만드는 **비지도 학습** 도구입니다.
* **DBN(Deep Belief Network):** RBM을 층층이 쌓아 올린 구조입니다. "문제집(정답)을 풀기 전에 교과서(데이터)를 여러 번 정독해서 기초를 다지는 것"과 같으며, 이를 통해 딥러닝이 다시 빛을 보게 되었습니다.

### 2. 텐서 조작의 기술 (3주차)
단순한 생성 외에도 차원을 자유자재로 다루는 능력이 시험에 자주 나옵니다.
* **`view(-1, 1)`의 마법:** `-1`은 "나머지 숫자에 맞춰서 자동으로 계산해줘"라는 뜻입니다. 예를 들어 12개의 원소가 있는 텐서에 `view(3, -1)`을 하면 자동으로 `(3, 4)`가 됩니다.
* **Broadcasting(브로드캐스팅):** 모양이 다른 두 텐서를 연산할 때, 파이토치가 똑똑하게 크기를 맞춰주는 기능입니다. (예: `(3, 1)` 텐서와 `(1, 4)` 텐서를 더하면 자동으로 `(3, 4)`가 되어 계산됨)
* **`squeeze()`와 `unsqueeze()`:** 1인 차원을 없애거나(squeeze) 새로 만드는(unsqueeze) 함수로, 모델의 입력 형식을 맞출 때 필수입니다.



### 3. 선형 계층(Linear Layer)의 수학적 진실 (4주차)
* **Affine Transformation:** 우리가 흔히 말하는 `y = Wx + b`는 수학적으로 '선형 변환'이 아니라 '아핀 변환'입니다. 상수항인 **Bias(b)**가 더해져 원점을 지나지 않을 수 있기 때문입니다.
* **가중치(Weight)와 편향(Bias):** 가중치는 입력 데이터의 **기울기(중요도)**를 결정하고, 편향은 결과값을 전체적으로 위아래로 **이동**시켜 정답에 가깝게 조정합니다.

### 4. 경사하강법과 학습률(Learning Rate) (5~6주차)
* **Local Minima vs Global Minima:** 우리가 찾고 싶은 곳은 전체에서 가장 낮은 골짜기(Global)지만, 학습률이 너무 작으면 작은 구덩이(Local)에 빠져서 못 나올 수 있습니다. 반대로 너무 크면 골짜기를 건너뛰어 버립니다(Overshooting).
* **오토그래드(AutoGrad):** `backward()` 함수 한 줄로 복잡한 미분 계산을 끝냅니다. 이때 **계산 그래프(Computational Graph)**라는 지도를 그려서 거꾸로 추적해 나갑니다.

### 5. 비선형 활성 함수의 전쟁 (6~7주차)
단순히 선형 계층만 쌓으면 아무리 깊게 쌓아도 결국 하나의 큰 선형 계층과 같습니다 ($2 \times 3 \times 4 = 24$인 것처럼요). 그래서 **비선형성**을 주입해야 합니다.
* **Sigmoid:** $0 \sim 1$ 사이로 압축. 미분 최댓값이 0.25라 층이 깊어지면 기울기가 금방 사라집니다.
* **ReLU:** $0$보다 작으면 $0$, 크면 그대로. 계산이 빠르고 기울기 소실을 완벽히 해결하지만, 음수 영역에서 뉴런이 죽어버리는(Dead Neuron) 문제가 있습니다.
* **LeakyReLU:** ReLU의 단점을 보완해 음수 쪽에도 아주 작은 기울기를 남겨둡니다.



### 6. 확률적 경사하강법(SGD)과 최적화 (7주차)
* **미니배치(Mini-batch):** 수만 장의 사진을 한 번에 학습시키면 GPU 메모리가 터집니다. 그래서 16장, 32장씩 쪼개서 학습시킵니다.
* **Epoch vs Step:**
    * **Epoch:** 전체 데이터를 한 번 다 본 것.
    * **Step(Iteration):** 미니배치를 한 번 학습시킨 것.
* **Adam 옵티마이저:** "가던 방향으로 계속 가려는 성질(Momentum)"과 "자주 변하는 값은 보폭을 줄이는 성질(Adaptive LR)"을 합친 가장 똑똑한 알고리즘입니다.

### 7. 과적합(Overfitting)과 평가 (7주차)
* **과적합:** 공부를 너무 열심히 해서 문제집의 오타까지 외워버린 상태입니다. 새로운 문제(Test Data)를 못 풉니다.
* **Dropout:** 학습할 때 무작위로 뉴런을 몇 개 끄고 학습시켜서 특정 뉴런에 의존하지 않게 만듭니다.
* **평가 모드(`model.eval()`):** 테스트할 때는 드롭아웃 같은 학습용 기술을 꺼야 합니다. 또한 `with torch.no_grad()`를 써서 불필요한 미분 계산을 막아 속도를 높입니다.



### 8. 코딩 꿀팁 (시험 및 실무)
* **`df.iloc` vs `df.values`:** 데이터를 정규화한 뒤 다시 넣을 때 `df.values`를 쓰면 복사본에만 적용되어 원본 데이터프레임이 안 변할 수 있습니다. 안전하게 **`df.iloc`**를 사용하세요.
* **`detach()`와 `item()`:** 손실(Loss) 값을 출력하거나 저장할 때는 `loss.item()`을 써서 텐서에서 숫자만 꺼내야 메모리 누수를 막을 수 있습니다.


## 계산

시험 대비 계산 문제 핵심 개념과 수식을 Markdown(.md) 파일 형식으로 정리해 드립니다. 아래 내용을 복사해서 저장하시거나, 함께 생성해 드린 파일을 다운로드하여 사용하세요.
### AI_Calculation_Summary.md 파일이 준비되었습니다
[file-tag: AI_Calculation_Summary.md]
## 📋 인공지능 개론 계산 및 수식 핵심 요약
### 1. 행렬 연산과 선형 계층 (Matrix & Linear Layer)
딥러닝의 가장 기초가 되는 연산입니다.
 * **행렬 곱 차원 결정 규칙:**
   
   * 앞 행렬의 **열(Column)** 개수와 뒤 행렬의 **행(Row)** 개수가 반드시 일치해야 연산이 가능합니다.
 * **아핀 변환 (Affine Transformation):**
   
   * W: 가중치(Weight), b: 편향(Bias).
   * 딥러닝의 선형 계층은 실제로는 원점을 지나지 않을 수 있는 아핀 변환입니다.
### 2. 활성 함수 (Activation Functions)
출력값에 비선형성을 부여하여 복잡한 문제를 풀 수 있게 합니다.
 * **시그모이드 (Sigmoid):** 결과를 0과 1 사이로 압축합니다.
   
 * **ReLU (Rectified Linear Unit):** 음수는 0으로, 양수는 그대로 통과시킵니다.
   
 * **소프트맥스 (Softmax):** 다중 분류에서 각 클래스 확률의 총합을 1로 만듭니다.
   
### 3. 손실 함수 (Loss Functions)
모델의 예측이 정답과 얼마나 다른지 수치화합니다.
 * **MSE (Mean Squared Error, 평균 제곱 오차):** 회귀 문제에서 사용합니다.
   
 * **BCE (Binary Cross Entropy):** 이진 분류 문제에서 사용합니다.
   
### 4. 경사하강법 및 최적화 (Gradient Descent & Optimization)
기울기를 따라 가중치를 수정하여 오차를 줄여나가는 과정입니다.
 * **가중치 업데이트 수식:**
   
   * \eta (Eta): 학습률(Learning Rate), \nabla L: 손실 함수의 기울기(Gradient).
 * **데이터 정규화 (Standardization):**
   
   * \mu: 평균, \sigma: 표준편차. 데이터의 범위를 일정하게 맞추어 학습을 돕습니다.
### 5. 기타 필수 기호 (Math Notation)
 * **수열의 합 (Sigma):** \sum_{i=1}^{n} x_i = x_1 + x_2 + \dots + x_n
 * **수열의 곱 (Product):** \prod_{i=1}^{n} x_i = x_1 \times x_2 \times \dots \times x_n
 * **Argmax:** 함수값을 최대화하는 입력값 x를 찾습니다.
   
**파일 활용 팁:** 이 내용은 주차별 강의 자료에 명시된 핵심 수식들을 기반으로 작성되었습니다. 특히 **행렬의 차원 계산**과 **가중치 업데이트 수식**은 코딩 문제와 연계되어 나올 가능성이 높으니 반드시 숙지하세요!

---

## 코딩

시험에서 코딩 문제는 주로 **"코드의 빈칸 채우기"**, **"실행 결과 예측"**, 또는 **"잘못된 부분 찾아내기"** 형태로 출제됩니다. 파이토치(PyTorch)와 판다스(Pandas)를 활용한 딥러닝 구현 핵심 코드를 정리해 드립니다.
### 1. 파이토치 학습 루프의 '5단계 공식' (가장 중요)
시험에 가장 많이 나오는 유형은 학습 루프의 순서를 맞추거나 빈칸을 채우는 문제입니다.
```python
for epoch in range(n_epochs):
    # 1. 예측 (Forward)
    y_hat = model(x)
    
    # 2. 손실 계산 (Loss)
    loss = F.mse_loss(y_hat, y)
    
    # 3. 기울기 초기화 (Zero Grad) - 이거 빼먹는 코드가 '잘못된 코드 찾기' 단골 문제입니다.
    optimizer.zero_grad()
    
    # 4. 역전파 (Backward) - 기울기 계산
    loss.backward()
    
    # 5. 가중치 업데이트 (Step) - 실제 W, b 수정
    optimizer.step()

```
 * **주의:** optimizer.zero_grad()를 하지 않으면 이전 에포크의 기울기가 누적되어 학습이 산으로 갑니다.
### 2. 데이터 전처리 및 텐서 변환
데이터를 모델에 넣기 전 처리 과정에서 실수하기 쉬운 코드들입니다.
 * **Pandas iloc vs values:**
   * df.values[:, :-1] = ... (X): 복사본에 값을 쓰는 경우가 있어 원본 데이터프레임이 안 변할 수 있습니다.
   * df.iloc[:, :-1] = ... (O): 원본 데이터프레임의 값을 직접 수정하는 안전한 방법입니다.
 * **Numpy to Tensor:**
   ```python
   # float()를 붙여서 연산이 가능한 실수형 텐서로 확실히 바꿔줘야 합니다.
   x = torch.from_numpy(df.values).float()
   
   ```
### 3. 텐서 조작 (Shape Manipulation)
차원을 계산하는 문제는 무조건 출제된다고 보시면 됩니다.
 * **view(-1, K):** 전체 원소 개수를 유지하며 모양을 바꿉니다.
   * 예: (12,) 크기의 텐서 x에 대해 x.view(3, -1) 결과는? **(3, 4)**
   * 예: (12,) 크기의 텐서 x에 대해 x.view(-1, 1) 결과는? **(12, 1) (세로로 긴 행렬)**
 * **squeeze()와 unsqueeze():**
   * unsqueeze(dim=1): 특정 위치에 1인 차원을 추가합니다. (예: [10] -> [10, 1])
   * squeeze(): 차원 중 1인 것을 모두 제거합니다. (예: [1, 10, 1] -> [10])
### 4. 모델 정의 (nn.Module)
클래스로 모델을 만들 때 반드시 지켜야 할 규칙입니다.
```python
class MyModel(nn.Module): # 반드시 nn.Module을 상속받아야 함
    def __init__(self, input_dim, output_dim):
        super().__init__() # 부모 클래스 초기화 필수
        self.linear = nn.Linear(input_dim, output_dim)
    
    def forward(self, x):
        # 학습 시 자동으로 실행되는 부분
        return self.linear(x)

model = MyModel(3, 1)
# 호출할 때는 model.forward(x)가 아니라 model(x)라고 씁니다.

```
### 5. 평가 및 메모리 관리
테스트 코드에서 자주 나오는 키워드입니다.
 * **model.eval():** 드롭아웃(Dropout)이나 배치 정규화(Batch Norm) 같은 기능을 '평가 모드'로 전환합니다.
 * **with torch.no_grad():** 테스트 시에는 기울기를 계산할 필요가 없으므로 메모리 절약을 위해 반드시 감싸줍니다.
 * **item()과 detach():**
   * loss.item(): 텐서 안에 들어있는 숫자 값 하나만 파이썬 숫자로 꺼냅니다. (로그 기록용)
   * x.detach(): 현재 계산 그래프에서 이 텐서를 떼어내어 더 이상 미분이 전파되지 않게 합니다.
### 6. 실전 코딩 문제 Tip
 * **문제:** "다음 중 학습 결과가 수렴하지 않는 이유는?"
   * **정답 후보:** optimizer.zero_grad() 누락, 학습률(lr)이 너무 큼, 활성 함수(ReLU 등) 누락.
 * **문제:** "입력 차원이 8이고 출력 차원이 1인 선형 계층의 코드는?"
   * **정답:** nn.Linear(8, 1)
 * **문제:** "분류 문제의 마지막 층에서 확률값을 출력하기 위해 사용하는 함수는?"
   * **정답:** F.softmax() (다중 분류) 또는 torch.sigmoid() (이진 분류)
이 코드 패턴들을 눈에 익혀두시면, 코드의 논리적인 흐름을 묻는 문제에서 당황하지 않고 답을 찾으실 수 있을 것입니다! 추가로 특정 코드의 작동 원리가 궁금하시면 말씀해 주세요.
